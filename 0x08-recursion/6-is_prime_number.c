#include "main.h"
#include <stdio.h>

/**
 * is_prime_number - print the code
 * @n: number
 * Return: int fact
 */
int is_prime_number(int n);
{
	if (n == 1)
	{
		return (0);
	}
	else if (y == 2)
	{
		return (1);
	}
	else
	{
		return (x * _pow_recursion(x, y - 1));
	}
}
