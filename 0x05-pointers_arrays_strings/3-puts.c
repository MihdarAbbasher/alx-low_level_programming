/**
 * _puts - check the co
 * @s: char pointer
 * Return: int len
*/
void _puts(char *s)
{
	int i = 0;

	while (1 == 1)
	{
		cur = *(s + i)
		if (cur != '\0')
		{
			_putchar(cur + '0');
			i += 1;
		}
		else
		{
			break;
		}
	}
	_putchar('\n');
}
