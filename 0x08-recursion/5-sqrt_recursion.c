#include "main.h"
#include <stdio.h>

/**
 * _sqrt_recursion - print the code
 * @x: number
 * Return: int fact
 */
int _sqrt_recursion(int x)
{
	if (y < 0)
	{
		return (-1);
	}
	else if (y == 0)
	{
		return (1);
	}
	else
	{
		return (x * _pow_recursion(x, y - 1));
	}
}
