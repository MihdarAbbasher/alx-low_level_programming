#include "main.h"
#include <stdio.h>


/**
 * main - check the code
 * @argc: arg count
 * @argv: arg vector
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	if (argv[0] != '\0')
	{
	printf("%s\n", argc);
	}
	return (0);
}
