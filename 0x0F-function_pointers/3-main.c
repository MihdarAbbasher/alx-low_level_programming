#include <stdio.h>
#include <stdlib.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(int ac, char *argv[])
{
	if (ac != 3 && argv[0] != NULL)
	{
		printf("Error\n");
		exit(98);
	}    
	    
	return (0);
}

